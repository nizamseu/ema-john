import React from 'react';


const Order = () => {
    const handleOrder=()=>{
    }
    return (
        <div>
            <h1>Order Section</h1>
            <button onClick={handleOrder}>Add Inventory</button>
        </div>
    );
};

export default Order;